<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Upload Success</title>
  <style>
     body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 20px;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      display:flex;
      flex-direction:column;
      align-items:center;
      justify-content:center;
      text-align:center;

    }
  </style>
</head>
<div>

  <h1>Image Uploaded and Resized!</h1>
  <% if (imageUrl) { %>
    <img src="<%= imageUrl %>" alt="Resized Image" />
    <p><%= message1 %></p>
    <p><%= message2 %></p>
  <% } %>
  <a href="https://imageoptimizer.onrender.com/"> upload and test another image</a>
  
  </div>
</body>
</html>
